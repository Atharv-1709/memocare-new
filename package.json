{
  "name": "memocare",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Accessible, offline-capable memory and medication assistance PWA.",
  "scripts": {
    "check": "node scripts/check-static.mjs",
    "test": "node --test tests/*.test.js && npm run check"
  },
  "devDependencies": {}
}
